sed -i '/val showWorldClockGlobe by/a\    val worldClockGlobeStyle by viewModel.worldClockGlobeStyle.collectAsState()' app/src/main/java/com/example/ui/screens/settings/SettingsScreen.kt
sed -i '/onCheckedChange = { viewModel.setShowWorldClockGlobe(it) }/a\                    )\n                    if (showWorldClockGlobe) {\n                        Column(modifier = Modifier.padding(horizontal = 16.dp, vertical = 12.dp)) {\n                            Text(text = "Globe Style", style = MaterialTheme.typography.titleMedium)\n                            Spacer(modifier = Modifier.height(12.dp))\n                            SingleChoiceSegmentedButtonRow(modifier = Modifier.fillMaxWidth()) {\n                                SegmentedButton(\n                                    selected = worldClockGlobeStyle == 0,\n                                    onClick = { viewModel.setWorldClockGlobeStyle(0) },\n                                    shape = SegmentedButtonDefaults.itemShape(index = 0, count = 2)\n                                ) { Text("Default Blue") }\n                                SegmentedButton(\n                                    selected = worldClockGlobeStyle == 1,\n                                    onClick = { viewModel.setWorldClockGlobeStyle(1) },\n                                    shape = SegmentedButtonDefaults.itemShape(index = 1, count = 2)\n                                ) { Text("Realistic Map") }\n                            }\n                        }\n                    }' app/src/main/java/com/example/ui/screens/settings/SettingsScreen.kt
